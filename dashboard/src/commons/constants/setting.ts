const {
    NODE_ENV,
    TMS_APP,
    VITE_AUTHENTICATION_KEY,
    VITE_GQL_ENDPOINT,
    VITE_GQL_BASE_URL,
    VITE_GQL_PATH,
    DOMAIN_APP,
} = import.meta.env;

export default Object.freeze({
    NODE_ENV,
    TMS_APP,
    VITE_AUTHENTICATION_KEY,
    VITE_GQL_ENDPOINT,
    VITE_GQL_BASE_URL,
    VITE_GQL_PATH,
    DOMAIN_APP,
});
